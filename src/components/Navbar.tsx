import { FC } from "react";

const Navbar: FC = () => {
    return (
        <div className="w-full py-6 text-center">
            <div className="container py-4">
                <h1 className="font-mono text-2xl font-bold text-gray-400 uppercase">
                    Aaron Carmen
                </h1>
            </div>
        </div>
    );
};

export default Navbar;
